version: '3.8'

services:
  postgres_test:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: test_user
      POSTGRES_PASSWORD: test_password
      POSTGRES_DB: iso_doc_platform_test
    ports:
      - "5433:5432"
    tmpfs:
      - /var/lib/postgresql/data
